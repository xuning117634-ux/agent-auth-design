
评审及决策点1： Agent 注册与身份治理
Agent 统一在 EAMAP 注册，形成公司级 Agent 身份目录。
IAM 负责 Agent 身份管理，明确 Agent 的身份标识、调用主体和权限边界。
IDaaS 负责用户身份令牌设计与校验，保证用户身份来源可信。

评审及决策点2： Agent 接入边界
Agent 不直接访问企业机机 API，核心业务资源必须使用人机API调用。
所有 Agent 工具调用必须经过 MCP 网关，不允许绕过网关直连资源侧。

评审及决策点3：凭证与身份隔离
Agent 不持有用户 Cookie、密钥等原始凭证。
用户凭证由Agent网关拦截隔离，Agent 只使用受控 Token 发起工具调用。


评审及决策点4：统一策略管控

云侧和端侧Agent进入同一套授权主链路。
Agent 身份、工具策略、用户策略、会话授权统一进入策略中心判定。
策略中心输出明确结果：允许、待用户授权、拒绝。


评审及决策点5：动态委托授权
高危工具不能默认继承用户全部权限。
敏感操作需要人在回路确认，只对当前会话或当前授权范围生效。
用户授权后仍需重新经过 MCP 网关和策略中心判定，不能绕过完整鉴权。

评审及决策点6：最小权限与工具分级
Agent 只能调用已绑定工具。
工具按风险等级区分：低风险可直接调用，高风险需用户确认。
未绑定工具、无用户访问权限、策略异常时默认拒绝。

评审及决策点7：网关执行与资源访问控制
策略中心负责决策，MCP 网关负责执行拦截。
只有策略中心返回允许后，MCP 网关才可继续访问 MCP Server 和资源侧。

评审及决策点8：Agent调用审计
每次调用记录用户、Agent、工具、会话、决策结果、调用链路。
授权确认、策略变更、异常拒绝都要进行审计记录。

总体评审决策：Agent 接入企业系统时，是否形成“统一注册、身份可信、凭证隔离、策略统一、动态授权、全链路审计”的安全闭环，并可以形成标准的 Agent 安全接入规范。