# 应用交互模式变化带来的安全真空：第 9 页已具备工具调用权限正常调用链路提示词

## 使用方式

把下面的提示词复制到图片/PPT 生成工具中。目标是生成一页 16:9 中文企业汇报 PPT 图：按照最新草图的空间布局重画“已具备工具调用权限正常调用链路”，但视觉更美观、更规整。页面继承前 8 页的蓝紫科技风、标题字体和纯白背景规则，同时保持极简，不增加风险卡片、方案卡片或复杂控制面。

建议输出文件路径：

```text
D:\Code\agent-auth-design\outputs\app-interaction-security-vacuum-slide-09-normal-tool-call-minimal-prompt.md
```

## 可直接复制的提示词

请生成一页 16:9 中文企业汇报 PPT 页面。

页面主题：

```text
已具备工具调用权限正常调用链路
```

副标题：

```text
用户已具备工具调用权限后，agent中台经agent网关换取受控 Token，MCP 网关完成权限决策与 Cookie 转换后访问目标工具。
```

重要要求：

- 页面左上角小标题标识写：`09 / 正常链路`。
- 标题字体、字号、颜色对齐第 4-8 页材料：小标题标识为亮蓝色，主标题为深海军蓝大号粗体。
- 画布背景必须是纯白色 `#FFFFFF`。
- 必须按照最新草图的空间关系排布，不要自由重排，不要把节点自动排成一条横向直线。
- 本页是极简链路页，只画节点、箭头、步骤编号、凭证标签和一个小图例。
- 不放风险卡片、现状问题卡片、解决方案卡片、能力卡片或结论带。
- 所有背景色、渐变和强调色必须放进明确边界的节点、标签、箭头或边框内部。

## 核心图示

本页要画成“最新草图的美化版”。固定节点如下：

```text
人类用户
agent中台
agent网关
agent权限策略中心
agent
MCP网关
目标工具
```

固定空间布局如下，必须遵守：

```text
左上：人类用户
左下：agent中台
中下：agent网关
中上：agent权限策略中心
右下：agent
右中：MCP网关
右上：目标工具
```

更精确的相对位置要求：

- `人类用户` 放在左侧上方，`agent中台` 放在它正下方，形成左侧上下入口。
- `agent网关` 放在 `agent中台` 右侧，保持同一水平线。
- `agent` 放在 `agent网关` 右侧，保持同一水平线。
- `MCP网关` 放在 `agent` 正上方，形成竖向调用关系。
- `目标工具` 放在 `MCP网关` 正上方，形成竖向工具访问关系。
- `agent权限策略中心` 放在 `agent网关` 上方、`MCP网关` 左侧，和 `MCP网关` 之间用水平往返箭头表达权限决策与鉴权通过。
- 图例放在右下角，不遮挡主链路。
- 不要把 `agent权限策略中心` 放到页面最中央，不要把 `agent` 和 `MCP网关` 拉成横向一排。

主请求链路：

```text
人类用户 -> agent中台 -> agent网关 -> agent -> MCP网关 -> 目标工具
```

策略决策链路：

```text
MCP网关 -> agent权限策略中心 -> MCP网关
```

凭证转换链路：

```text
agent中台 -> agent网关：agent发起代理转发；辅助注释：携带用户Cookie
agent网关 -> agent：Exchange token；辅助注释：本次调用工具权限范围
agent网关 -> MCP网关：转换Cookie
MCP网关 -> 目标工具：调用API工具；辅助注释：Header: Cookie
```

返回链路：

```text
目标工具 -> MCP网关 -> agent -> agent网关 -> agent中台 -> 人类用户
```

## 链路步骤

图中步骤编号固定为 13 个，必须按下面顺序编号。每个步骤可以拆成“主文字 + 辅助注释”两层：

```text
1. 访问智能体
   task任务
2. agent发起代理转发
   携带用户Cookie
3. Exchange token
   本次调用工具权限范围
4. 请求MCP网关
   携带Token
5. 权限决策
6. 鉴权通过
7. 转换Cookie
8. 调用API工具
   Header: Cookie
9. 工具响应
10. 返回工具调用信息
11. agent请求响应
12. agent代理请求响应
13. 智能体响应
```

步骤文字层级要求：

- 只区分同一步骤内部的主文字与辅助注释，不再按步骤编号设置额外强弱。
- 所有步骤主文字使用统一层级：同一字号、同一字重、同一清晰度，颜色可跟随箭头语义但不要人为分重点。
- 所有辅助注释必须更小、更浅、更细，不要和主文字同等强调。
- 示例：`1. 访问智能体` 是主文字，`task任务` 是辅助注释；`4. 请求MCP网关` 是主文字，`携带Token` 是辅助注释。

步骤放置要求：

- `1. 访问智能体` 放在 `人类用户 -> agent中台` 的向下箭头旁边，辅助注释只写 `task任务`，放在主文字下方。
- `2. agent发起代理转发` 放在 `agent中台 -> agent网关` 的水平箭头上方，辅助注释 `携带用户Cookie` 放在主文字下方。
- `3. Exchange token` 放在 `agent网关 -> agent` 的水平箭头上方，辅助注释 `本次调用工具权限范围` 放在主文字下方。
- `4. 请求MCP网关` 必须放在 `agent -> MCP网关` 的蓝色竖向向上箭头左侧或贴近箭头中段，视觉上绑定到 agent 向 MCP 网关发起请求的上行箭头；辅助注释 `携带Token` 放在主文字下方。不要把第 4 步放到 `MCP网关` 右侧，也不要靠近绿色下行返回箭头。
- `5. 权限决策` 放在 `MCP网关 -> agent权限策略中心` 的水平箭头旁边。
- `6. 鉴权通过` 放在 `agent权限策略中心 -> MCP网关` 的绿色返回箭头旁边。
- `7. 转换Cookie` 放在 `agent网关 -> MCP网关` 的绿色斜向箭头旁边，位置靠近斜线中段。
- `8. 调用API工具` 放在 `MCP网关 -> 目标工具` 的竖向向上箭头旁边，辅助注释 `Header: Cookie` 放在主文字下方。
- `9. 工具响应` 放在 `目标工具 -> MCP网关` 的绿色竖向向下箭头旁边。
- `10. 返回工具调用信息` 放在 `MCP网关 -> agent` 的绿色竖向向下箭头旁边。
- `11. agent请求响应` 放在 `agent -> agent网关` 的绿色水平返回箭头旁边。
- `12. agent代理请求响应` 放在 `agent网关 -> agent中台` 的绿色水平返回箭头旁边。
- `13. 智能体响应` 放在 `agent中台 -> 人类用户` 的绿色向上箭头旁边。

## 布局草图

生成时请按下面的结构理解空间关系，实际视觉要更美观：

```text
                         目标工具
                           ↑ 8. 调用API工具
                           │    Header: Cookie
                           ↓ 9. 工具响应
agent权限策略中心  <---- 5. 权限决策 ----  MCP网关
        ---- 6. 鉴权通过 ---->              ↑
                                            │ 4. 请求MCP网关
                                            │    携带Token
                                            ↓ 10. 返回工具调用信息

人类用户                                      agent
   ↓ 1. 访问智能体                            ↑
     task任务
   ↑ 13. 智能体响应                           |
agent中台 ---- 2. agent发起代理转发 ---- agent网关 ---- 3. Exchange token ---- agent
              携带用户Cookie                            本次调用工具权限范围
            <---- 12. agent代理请求响应 ----       <---- 11. agent请求响应 ----

agent网关  ---------- 7. 转换Cookie ----------> MCP网关
```

注意：这只是布局说明，不要把它画成代码块样式；最终页面应使用现代 PPT 节点和箭头。

## 视觉美化规则

- 整体是最新草图的现代化重绘：布局不变，线条更规整，节点更精致。
- 背景：纯白色 `#FFFFFF`。
- 节点：白底、蓝色或青色细边框、8px 左右圆角，内部可以有轻量图标。
- `agent` 节点可使用蓝紫轻微渐变，但渐变必须只在节点框内。
- `MCP网关` 可使用浅青色边框或浅青色节点底色。
- `agent权限策略中心` 使用白底蓝边框，图标可用盾牌或策略图标。
- `目标工具` 使用扳手、工具或服务图标，保持白底蓝边框。
- 请求链路使用蓝色或深灰细箭头，线宽统一。
- 鉴权通过、Cookie 转换、响应链路使用绿色细箭头。
- `7. 转换Cookie` 使用绿色斜向箭头或绿色虚线斜箭头，从 `agent网关` 指向 `MCP网关`。
- `8. 调用API工具` 和 `9. 工具响应` 在 `MCP网关` 与 `目标工具` 之间形成上下双向箭头：上行调用、下行返回；`Header: Cookie` 只作为第 8 步的小号辅助注释。
- `10. 返回工具调用信息` 放在 `MCP网关 -> agent` 的绿色下行箭头旁边。
- `11. agent请求响应`、`12. agent代理请求响应` 沿底部主链路下方水平向左返回，线条不要贴近页面底边。
- `13. 智能体响应` 放在 `agent中台 -> 人类用户` 的绿色上行箭头旁边，和第 1 步形成清晰的竖向双向关系。
- 箭头用直线或 90 度折线，不要使用大弧线，不要线条绕远，不要出现多余支线。
- 步骤文字只做主辅层级：所有步骤主文字保持统一字号、统一字重和统一清晰度，不按步骤编号做额外突出。
- 所有换行注释都要弱化：字号比主文字小 2-4pt，颜色使用浅灰或低饱和色，字重 regular，不要加粗。
- 第 4 步必须贴近 `agent -> MCP网关` 的蓝色上行箭头中段，主文字 `4. 请求MCP网关` 在上，注释 `携带Token` 在下；不要放在 `MCP网关` 右侧，不要和 `10. 返回工具调用信息` 的绿色下行箭头混在一起。
- 图例放在右下角，三行即可：
  - `蓝色：请求链路`
  - `绿色：鉴权通过 / 响应`
  - `虚线：凭证转换`
- 图例本身不要做大卡片，只用小线段加小字，保持轻量。
- 主图区域留白充足，节点之间距离均匀，文字不要压线。

## 白底与框内颜色规则

- 整个画布背景必须是纯白色 `#FFFFFF`。
- 所有框框外的区域保持纯白。
- 背景色、渐变、强调色只允许出现在节点内部、标签内部、图标内部、箭头、边框或图例线段中。
- 不要使用整页浅灰、浅蓝、渐变或纹理背景。
- 不要使用大外层容器框包住整张图。
- 若使用节点阴影，只能非常轻微，不能让框外白底变灰。

## 图中文字上限

最终页面中尽量只出现以下文字，不要自行扩写：

```text
09 / 正常链路
已具备工具调用权限正常调用链路
用户已具备工具调用权限后，agent中台经agent网关换取受控 Token，MCP 网关完成权限决策与 Cookie 转换后访问目标工具。

人类用户
agent中台
agent网关
agent权限策略中心
agent
MCP网关
目标工具

1. 访问智能体
task任务
2. agent发起代理转发
携带用户Cookie
3. Exchange token
本次调用工具权限范围
4. 请求MCP网关
携带Token
5. 权限决策
6. 鉴权通过
7. 转换Cookie
8. 调用API工具
Header: Cookie
9. 工具响应
10. 返回工具调用信息
11. agent请求响应
12. agent代理请求响应
13. 智能体响应

蓝色：请求链路
绿色：鉴权通过 / 响应
虚线：凭证转换
```

## 必须避免

- 不要改变最新草图的空间布局。
- 不要把所有节点排成一条水平链路。
- 不要把 `agent` 放到 `MCP网关` 左侧，`agent` 必须在 `MCP网关` 下方。
- 不要把 `目标工具` 放到 `MCP网关` 右侧，`目标工具` 必须在 `MCP网关` 上方。
- 不要把 `agent权限策略中心` 放到页面最上方居中，它必须靠近 `MCP网关` 左侧，服务于权限决策链路。
- 不要省略 `agent网关 -> MCP网关` 的 `7. 转换Cookie` 链路。
- 不要省略 `目标工具 -> MCP网关` 的 `9. 工具响应` 返回箭头。
- 不要把第 4 步放在 `MCP网关` 右侧或绿色返回箭头旁；第 4 步必须绑定到 `agent -> MCP网关` 的蓝色上行请求箭头。
- 不要把第 10 步写成最终给用户的响应；第 10 步必须是 `返回工具调用信息`。
- 不要省略 `agent中台 -> 人类用户` 的 `13. 智能体响应` 绿色上行箭头。
- 不要按步骤编号做额外强弱区分；所有步骤主文字保持同一层级。
- 不要把步骤换行后的注释文字做成和主文字一样醒目；注释必须更小、更浅、更轻。
- 不要使用上一版的旧入口节点或旧服务节点命名。
- 不要出现具体业务名称、外部厂商标识或产品 Logo。
- 不要出现供应计划、订单、调度、合同等具体业务语义。
- 不要画成第 8 页那种全景方案图，本页只讲正常调用链路。
- 不要放风险卡片、现状问题卡片、解决方案卡片、能力卡片或结论带。
- 不要复刻手绘图的粗糙线条、黑色粗框和随意排版，要画成正式 PPT 风格。
- 不要让连线穿过节点文字。
- 不要加入 A2A 多 agent 协作链路，本页只有单 agent 正常工具调用链路。

## 验收标准

生成结果必须满足：

- 一眼能看出这是“最新草图的美化版”，而不是重新设计的横向链路图。
- 页面包含 `09 / 正常链路` 和 `已具备工具调用权限正常调用链路`。
- 节点位置符合：左上 `人类用户`、左下 `agent中台`、中下 `agent网关`、右下 `agent`、右中 `MCP网关`、右上 `目标工具`、中上 `agent权限策略中心`。
- `agent` 位于 `MCP网关` 下方，`目标工具` 位于 `MCP网关` 上方。
- `agent权限策略中心` 与 `MCP网关` 之间有 `5. 权限决策` 和 `6. 鉴权通过` 两条方向相反的箭头。
- `agent网关 -> MCP网关` 有 `7. 转换Cookie` 链路。
- `MCP网关 -> 目标工具` 有 `8. 调用API工具` 上行箭头，`Header: Cookie` 作为小号辅助注释放在主文字下方。
- `目标工具 -> MCP网关` 有 `9. 工具响应` 下行箭头。
- `MCP网关 -> agent` 有 `10. 返回工具调用信息` 下行箭头。
- `agent -> MCP网关` 的蓝色上行箭头旁有 `4. 请求MCP网关`，辅助注释为 `携带Token`，位置不偏到 `MCP网关` 右侧。
- `agent -> agent网关` 有 `11. agent请求响应` 返回箭头。
- `agent网关 -> agent中台` 有 `12. agent代理请求响应` 返回箭头。
- `agent中台 -> 人类用户` 有 `13. 智能体响应` 上行箭头。
- 图中包含 7 个固定节点：`人类用户`、`agent中台`、`agent网关`、`agent权限策略中心`、`agent`、`MCP网关`、`目标工具`。
- 步骤编号完整包含 1-13，且顺序与本提示词一致。
- 所有步骤主文字保持同一层级，不按步骤编号做额外突出。
- 所有换行辅助注释都比主文字更小、更浅、更细，能看出主次关系。
- 页面足够克制，除主图和小图例外没有额外信息块。
- 画布背景为纯白色，所有颜色都在节点、标签、箭头、图标或边框内。
- 图中没有具体业务词、厂商 Logo、供应计划、订单、调度、合同等业务语义。

## 与前 8 页的承接

前 4-7 页逐个拆解 agent 访问链路中的问题，第 8 页汇总为云侧 agent 安全方案全景。本页进一步补充一个低信息密度的正常链路页，适合在讲完方案全景后说明：

```text
当用户已经具备工具调用权限时，调用链路不需要再展开复杂风险模型。
核心是让 Cookie、Token、权限决策和 MCP 调用都停留在受控网关链路中。
agent 只发起工具调用，不直接持有完整用户 Cookie。
MCP 网关在策略中心鉴权通过后，再合规调用目标工具。
```

本页作为第 8 页之后的“正常路径说明页”，帮助听众从全景方案回到一条极简、可执行、可审计的调用链路。

## 生成后的自检

生成图片后请检查：

- 页面是否仍然是纯白背景。
- 是否只保留极简链路，没有风险卡片和方案卡片。
- 是否保留最新草图的布局：左侧上下入口、中下 agent网关、右下 agent、右中 MCP网关、右上目标工具、中上策略中心。
- `人类用户`、`agent中台`、`agent网关`、`agent权限策略中心`、`agent`、`MCP网关`、`目标工具` 是否齐全。
- 13 个步骤标签是否都能对应到具体箭头。
- `agent` 是否在 `MCP网关` 下方，`目标工具` 是否在 `MCP网关` 上方。
- `访问智能体` 与辅助注释 `task任务` 是否主次分明，且第 1 步辅助文字只写 `task任务`。
- `agent发起代理转发` 与辅助注释 `携带用户Cookie`、`Exchange token` 与辅助注释 `本次调用工具权限范围` 是否表达清楚。
- 第 4 步 `请求MCP网关` 是否贴在 `agent -> MCP网关` 的蓝色上行箭头旁，辅助注释 `携带Token` 是否在主文字下方且更弱。
- 是否没有按步骤编号额外区分强弱，只保留主文字与辅助注释的强弱关系。
- 所有步骤的第二行注释是否使用更小、更浅、更细的样式。
- `MCP网关` 与 `agent权限策略中心` 之间是否有 `权限决策` 和 `鉴权通过` 两段。
- `目标工具` 和 `MCP网关` 之间是否同时有 `8. 调用API工具` 上行箭头和 `9. 工具响应` 下行箭头，`Header: Cookie` 是否作为第 8 步辅助注释。
- 返回链路是否依次为 `目标工具 -> MCP网关 -> agent -> agent网关 -> agent中台 -> 人类用户`。
- 节点之间是否有足够留白，连线是否没有穿过文字。
